import styled from 'styled-components'

const Text = styled.p`
`

Text.PageTitle = styled.h2`
  font-size: 3rem;
  line-height: 4rem;
`

Text.SectionTitle = styled.h3`
  font-size: 2rem;
  line-height: 4rem;
`

export default Text
